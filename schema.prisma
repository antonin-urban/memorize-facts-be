// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url      = env("DATABASE_URL")
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
  output   = "node_modules/.prisma/client"
}

model User {
  id       String  @id @default(uuid()) @postgresql.Uuid
  email    String  @unique @default("")
  password String
  isAdmin  Boolean @default(false)
}

model Fact {
  id          String     @id @default(uuid()) @postgresql.Uuid
  name        String     @default("")
  description String     @default("")
  deadline    DateTime?
  active      Boolean    @default(false)
  schedules   Schedule[] @relation("Fact_schedules")
  tags        Tag[]      @relation("Fact_tags")
  ownerId     String     @default("")
}

model Tag {
  id             String @id @default(uuid()) @postgresql.Uuid
  name           String @default("")
  ownerId        String @default("")
  from_Fact_tags Fact[] @relation("Fact_tags")
}

model Schedule {
  id                  String           @id @default(uuid()) @postgresql.Uuid
  name                String           @default("")
  type                ScheduleTypeType @default(NOTIFY_EVERY)
  scheduleParameters  Json?            @default("{\"interval\":10,\"notifyTimes\":[\"08:00:00+00:00\",\"12:00:00+00:00\",\"16:00:00+00:00\"],\"dayOfWeek\":[true,true,true,true,true,true,true]}")
  ownerId             String           @default("")
  from_Fact_schedules Fact[]           @relation("Fact_schedules")
}

enum ScheduleTypeType {
  NOTIFY_EVERY
  NOTIFY_AT
}